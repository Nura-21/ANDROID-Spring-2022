<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".InsertUpdate">

    <TextView
        android:id="@+id/InsertUpdateTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Insert ToDO"
        android:textColor="@color/black"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_margin="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <EditText
        android:id="@+id/EditTextTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Insert Title"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@+id/InsertUpdateTitle"
        app:layout_constraintStart_toStartOf="@+id/InsertUpdateTitle"
        app:layout_constraintEnd_toEndOf="@+id/InsertUpdateTitle"
        />

    <EditText
        android:id="@+id/EditTextDescription"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Insert Description"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/EditTextTitle"
        app:layout_constraintStart_toStartOf="@+id/EditTextTitle"
        app:layout_constraintEnd_toEndOf="@+id/EditTextTitle"
        />

    <Spinner
        android:id="@+id/spinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:spinnerMode="dropdown"
        app:layout_constraintTop_toBottomOf="@+id/EditTextDescription" />


    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/Save"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="24dp"
        android:backgroundTint="#808080"
        android:text="Save"
        android:textColor="@color/white"
        app:icon="@drawable/icon_done"
        app:iconTint="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>